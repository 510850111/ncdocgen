#!/usr/bin/env python
# -*- coding: utf-8 -*-
#
# -----------------------------------------------------------------------------
# 配置管理模块
# ============
# 定义文档生成过程中的配置项，包括：
# - 标签翻译表：将英文标签映射为中文显示
# - 默认值表：当注释项为空时使用的默认内容
#
# 作者: ncdocgen团队
# -----------------------------------------------------------------------------

# ========================================================================
# 标签翻译表
# ========================================================================
# 将doxygen风格的英文标签映射为中文显示文本
# 用于生成函数说明表中的标签列

comment_translate_table = {
    # 文件级别的标签
    'file'   : '文    件',  # 文件名
    'author' : '作    者',  # 作者信息
    'date'   : '日    期',  # 创建/修改日期
    
    # 函数级别的标签
    'proto'  : '原    型',  # 函数原型（声明）
    'brief'  : '概    述',  # 功能概述
    'param'  : '参    数',  # 参数说明
    'return' : '返 回 值',   # 返回值说明
    'fcall'  : '引    用',   # 调用的其他函数
    'gvarin' : '输入全局',   # 读取的全局变量
    'gvarout': '输出全局',   # 修改的全局变量
    'details': '说    明',   # 详细说明
    'depth'  : '深    度',   # 流程图展开深度
    
    # 其他标签
    'alias'  : '别    名',   # 别名/缩写
}

# ========================================================================
# 默认值表
# ========================================================================
# 当注释中没有指定某项内容时，使用这里的默认值
# '%s' 会被替换为实际的函数名

comment_null_table = {
    'brief'   : '函数%s的说明',  # 使用函数名填充
    'param'   : '无',             # 无参数
    'return'  : '无',             # 无返回值
    'fcall'   : '无',             # 无函数调用
    'gvarin'  : '无',             # 无输入全局变量
    'gvarout' : '无',             # 无输出全局变量
    'details' : '无特别说明',     # 无详细说明
    'ignore'  : 'y',              # 忽略标记
}
